import { GeneratedTest } from './types';

export async function generateTableImage(test: GeneratedTest): Promise<string> {
  console.log('Generating table image for test:', test.id);
  // Sử dụng cấu trúc thuần túy, loại bỏ hoàn toàn các thẻ component viết hoa gây lỗi
  return '';
}
